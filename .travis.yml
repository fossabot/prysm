language: python
python:
  - "3.6"
install:
  - pip install .
  - pip install -r requirements.txt
  - pip install coveralls
cache: pip
script:
  - pytest
  - coverage run --source=prysm setup.py test

notifications:
  email: false
after_success:
  - coveralls
